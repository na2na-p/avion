{
	"name": "avion",
	"version": "0.0.0",
	"repository": "https://github.com/na2na-p/avion",
	"author": "na2na",
	"private": true,
	"packageManager": "pnpm@8.2.0",
	"engines": {
		"pnpm": ">=8.2.0",
		"npm": "use pnpm instead.",
		"yarn": "use pnpm instead."
	},
	"scripts": {
		"build": "turbo build",
		"lint": "turbo lint",
		"test": "turbo test",
		"setup-backend": "cd packages/backend && cp .env.example .env",
		"setup-ci": "pnpm setup-backend",
		"setup": "pnpm setup-backend"
	},
	"devDependencies": {
		"turbo": "^1.9.1"
	}
}
