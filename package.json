{
	"name": "avion",
	"version": "0.0.0",
	"repository": "https://github.com/na2na-p/avion",
	"author": "na2na",
	"private": true,
	"packageManager": "pnpm@8.15.1",
	"engines": {
		"pnpm": ">=8.15.0",
		"npm": "use pnpm instead.",
		"yarn": "use pnpm instead."
	},
	"scripts": {
		"build": "turbo build",
		"lint": "turbo lint",
		"fmt": "turbo fmt",
		"test": "turbo test",
		"type-check": "turbo type-check",
		"setup-graphql": "cd packages/graphql && pnpm build",
		"setup-ci": "pnpm setup-graphql",
		"setup": "pnpm setup-graphql"
	},
	"devDependencies": {
		"turbo": "^1.12.2"
	}
}
